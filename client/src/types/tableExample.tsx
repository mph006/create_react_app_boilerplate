export type NumRecords = number

export type TableData = {
    key?: number
    readonly postId: number
    readonly id: number
    readonly name: string
    readonly email: string
    readonly body: string
}